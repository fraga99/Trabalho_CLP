# Trabalho_CLP
Trabalho de implementação da matéria de Conceitos de Linguagem de Programação

# Diogo Avila Fraga
# 20103419

# Necessário
python:
import 
import

# Comandos
gcc -shared -o mandelbrot.dll -fPIC mandelbrot.c
python interface.py

# Caso tenha apenas o mingw 32 (Meu caso), baixar o mingw 64, adicionar ao PATH e usar:
C:\mingw64\bin\gcc -shared -o mandelbrot.dll -fPIC mandelbrot.c
